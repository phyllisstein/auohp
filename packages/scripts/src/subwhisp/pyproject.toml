[tool.poetry]
name = "subwhisp"
description = "A CLI for generating subtitles from audio files."
version = "0.0.0"
authors = ["Daniel P. Shannon <daniel@try.works>"]

[tool.poetry.dependencies]
python = "^3.10"
loguru = "^0.7.2"
"pyannote.audio" = "^3.1.0"

[tool.poetry.scripts]
subwhisp = "subwhisp.cli:run"
